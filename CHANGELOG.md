# Changelog

## [Unreleased]

## [1.4.14] - 2026-09-24

### Fixed

- 区分证书固定指纹与客户端 uTLS 指纹，有损通用转换明确拒绝，SSRVPN 自动接收完整 YAML。
- 校验协议必需凭据、字段类型与端口；无可用节点时保留正常缓存。
- 导出避开客户端保留名称，统一处理去重和引用；重命名最多 100 条、每步名称最多 512 字、订阅响应最多 8 MiB。
- 批量同步审计区分全部成功、部分完成和失败，记录计数与 UTC 时间；异常 JSON 返回 400。
- 调整主要按钮文字对比度，明确服务期内用户文案，更新跨 Worker 锁说明；拆分独立订阅导出模块。

### Added

- 正式安装入口验证源码归档 SHA-256、Sigstore 工作流身份和标签后执行；CI 传输签名产物后在服务器再次验证。
- 可选的真实代理访问校验与定时复测：固定并校验入口地址、保留证书固定参数、最多两个核心进程、单次 8 秒；每轮最多 32 节点和 45 秒。
- 实际代理结果按完整配置与凭据隔离；仅真实代理 HTTPS 204 成功时显示验证通过，链式代理保持明确的未验证状态。

### Compatibility

- 无数据库迁移和新增 Python 生产依赖。分享地址、用户数据和已有规则保留；超限旧规则会明确拒绝导出，可在设置页修正。
- 正式更新使用 `install-release.sh v1.4.14`；本地受信任源码仍可用 `deploy.sh`，官方标签的未签名 Git 拉取入口会拒绝执行。
- 新监控能力需管理员运行 `install-monitor.sh` 启用；使用固定哈希的 Mihomo 1.19.31，增加约两个核心进程的资源开销。暂停或回滚至不含监控脚本的版本时，定时任务自动跳过。

## [1.4.13] - 2026-09-24

### Fixed

- Caddy 在请求进入应用线程前缓冲完整请求体，并限制大小、请求头和请求体读取时间；增加真实慢请求与超限上传回归。
- 修复仪表盘健康状态刷新脚本异常、专线停用后服务状态误报、最大到期日期导致详情页崩溃，以及分享说明与实际过滤不一致。
- 对订阅重命名后的名称去重，同时更新链式代理引用；屏蔽、缺失、歧义或循环依赖不会降级为直连。
- 完整保存 Clash 节点参数；补齐通用链接的 ALPN 和 TUIC 参数；不能完整转换时明确要求兼容格式，SSRVPN 自动使用 YAML。
- 批量同步按 4 个账号分批拉取和提交，降低峰值内存；任务锁跨 Worker 生效，进程退出后自动释放。
- 健康恢复命令添加超时，单元预算提高到 90 秒，覆盖双组件恢复及复查。
- 统一北京时间显示，分享弹窗增加实际输出数量、节点名称和 Clash 链接。

### Compatibility

- 数据库迁移 8 为节点增加完整配置字段；已有缓存继续可用，下次成功同步后保存完整参数。无新增运行依赖。
- 通用格式无法表达的节点参数返回 HTTP 406，改用 `?format=clash`；普通订阅地址及分享令牌保持兼容。
- 默认 Caddy 读取预算为请求头 10 秒、请求体 30 秒、空闲连接 2 分钟。共用监听器的站点也受读取预算影响；已有自定义服务器选项须自行设置这些期限，部署会保留其配置并拒绝不满足预算的更新。

## [1.4.12] - 2026-09-19

### Added

- 订阅设置增加节点关键词屏蔽总开关，按原始节点名称匹配，支持多个关键词与英文大小写忽略。
- 账号分享、用户分享和管理端合并订阅统一过滤；兼容 Base64 与 Clash，关闭后恢复输出，不删除节点。

### Fixed

- 首页节点待办改为数量汇总和监控入口，不再渲染全部节点明细，减少浏览器状态数据。

### Compatibility

- 数据库迁移 7 新增屏蔽设置表，默认关闭；无新增依赖。全部匹配时返回空订阅，客户端可能保留旧节点。

## [1.4.11] - 2026-09-16

### Changed

- 监控页按入口及连接参数合并重复账号节点，每组仅展示、检测一个稳定代表节点，并显示关联账号与配置数量。
- SNI、TLS 验证模式、协议或传输参数不同的节点保持独立，无法解析的配置不参与合并。
- 复用现有单项和批量检测接口，不复制账号认证结果，不改动账号详情、订阅数据或数据库结构。

## [1.4.10] - 2026-09-16

### Fixed

- 旧版检测记录显示“旧版结果待复测”，说明需点击检测取得分层结果，避免与新结果超时混淆。
- 批量检测优先处理待确认及最早尝试的节点，达到每轮两分钟上限后可继续剩余项目，避免大列表反复检测前几项。
- 批量请求增加 400 毫秒间隔，保留最多四路并发与原有限流；遇到限流遵循 Retry-After 暂停并保留待测队列和已有证据。
- 监控、账号详情及仪表盘使用服务器计算的结果年龄和浏览器单调计时，避免设备时钟偏差将新结果误判为过期。

### Compatibility

- 无数据库迁移或新依赖，仍采用 15 分钟有效期与手动复测，不改变代理验证能力或出站安全边界。

## [1.4.9] - 2026-09-16

### Fixed

- 节点检测不再把 TCP/TLS 入口可达或 TLS 异常标为代理在线；分层记录 DNS、TCP、TLS、代理认证和代理访问，未执行的阶段明确标注。
- TLS 默认执行证书和主机名验证，使用节点 SNI、ALPN；显式跳过证书验证的配置会标明验证模式。QUIC、REALITY、插件等未支持的部分不冒充成功。
- 监控页、账号详情与仪表盘统一结果语义、UTC 时间及 15 分钟有效期；旧在线记录仅作历史参考，异常保留上次证据。
- 检测按节点配置独立持久化；同步时仅在连接配置不变时保留结果，避免凭据、SNI 或传输变更沿用旧结果。
- 批量检测增加进度、并发与总时间限制，使用数据库短租约防止重复检测；手机宽度下检测信息纵向呈现。

### Compatibility

- 数据库 schema 6 为节点增加分层结果、最近尝试信息和探测租约表；迁移可重复执行，不将旧 `is_online=1` 升级为代理验证通过。
- 本版本仍只执行入口检测，没有任何协议完成代理认证或代理访问验证；API `online` 仅保留入口兼容含义，新调用方应读取 `health`。
- `/api/check-by-host` 仅接受已有且唯一的节点配置；同入口多配置返回 409，改用节点 ID 检测。

## [1.4.8] - 2026-09-15

### Fixed

- 表单仅返回受控校验文案；订阅解析失败不再回显原始内容或底层异常，避免密钥和内部信息出现在错误提示中。

- 批量同步区分全部成功、部分失败和全部失败，保留账号明细与处理入口；接口错误、登录失效及超时不再显示成功或自动刷新清除结果。
- 订阅导入、用户服务新增/编辑/续期支持原位提交，失败时保留输入并显示错误，成功后再跳转；保留原有普通表单及 CSRF 校验。
- 公共弹窗与手机侧栏隔离背景焦点，支持键盘循环、Escape 关闭和焦点恢复；兼容弹窗位于主内容内部的页面。
- 修复窄屏同步时间与按钮重叠；监控接口失败显示为未确认并保留上次结果，重试成功后更新状态和检测时间。

### Changed

- 首页待办保持四类整合，完整显示前两项并提供展开数量，取消内部小区域滚动；桌面概览改为右侧上下排列，手机统计区更紧凑。
- 首页续期直接打开对应用户的续期弹窗，成功后返回首页待办；用户服务增加微信号/专线搜索和状态筛选。
- 增大手机小按钮，压缩详情概览，精简技术术语，并支持减少动态效果。

### Tests

- 新增表单错误、重复记录、CSRF、安全返回和监控时间的回归测试；Node.js 可用时，常规测试同时执行前端同步及监控失败恢复测试。

## [1.4.7] - 2026-09-05

### Changed

- 分享订阅不再输出 `Subscription-Userinfo`，停止向客户端公开上传量、下载量、总配额和到期日期；同时覆盖账号分享、用户独立订阅、Base64 和 Clash YAML。
- 后台统计、首页提醒、用户服务开始/到期与暂停/停用限制继续生效，已有链接和节点配置保持兼容。
- 已导入订阅需在客户端更新；若客户端保留旧信息，可重新导入同一链接。

### Tests

- 验证多种客户端请求不再获取流量和到期字段，节点仍可解析，后台数据保持不变，未开始或已到期的用户服务仍返回 404。

## [1.4.6] - 2026-09-05

### Changed

- 首页「需要处理」合并用户续费、账号到期、流量告警与节点异常，移除独立续费大表格，分组折叠并在列表内滚动以节省空间。
- 用户与活跃上游账号的到期提醒包含未来 30 天及已过期项目；所有待办均可查看，计数不受原来的展示条数限制。
- 保留用户、专线、到期时间、最近提醒和续期入口，首页标记已提醒后返回首页；无待办文案覆盖全部四类提醒。

### Tests

- 覆盖到期日与 30 天边界、暂停和待生效排除、完整列表与总数、名称转义、CSRF、提醒记录及安全返回首页。

## [1.4.5] - 2026-09-05

### Added

- 面板左下角显示当前版本，新增「检查更新」按钮与正式发布说明入口；区分有更新、已是最新、本地版本较新及检查失败。
- 版本检查需要管理员登录和 CSRF，只请求固定 GitHub 公开接口，按数字比较版本；并发请求合并，网络失败可重试，不执行安装操作。

### Fixed

- 显式指定仓库或版本时优先拉取，修复安装目录中的旧源码覆盖新版本选择；正式版本必须是真实标签且与源码 VERSION 一致，停服前完成检查。
- 发布工作流校验标签与 VERSION 一致，避免发布版本与面板显示不符。
- 更新检查失败只返回固定中文提示，避免异常细节进入 API 响应。
- 一键部署示例使用私有临时目录，完整下载成功后才执行，避免空文件、部分下载或共享临时目录中的其他源码影响安装；同步修正文档中的输入顺序。
- 侧栏支持短屏滚动，新增版本信息后仍能访问底部操作。

### Tests

- 新增版本比较、鉴权、CSRF、并发缓存、网络与异常响应、真实 Git 标签选择和下载失败回归，并纳入 Ubuntu 24.04 部署门禁。

## [1.4.4] - 2026-09-05

### Fixed

- 启动与回滚恢复均等待数据库就绪接口成功，避免端口尚未监听就报告恢复完成；备份与健康检查的锁文件移入受保护目录。
- 补齐用户服务详情页的暂停、恢复和停用入口，明确停止订阅下载与撤销上游节点凭据的区别。
- 流量接口拒绝异常密码类型，并在累计流量越界时整体回滚批次或采集游标；绝对值上报的周期检查与更新在同一事务中执行。
- 续期串行核对最新到期日，避免并发请求缩短服务；同步拒绝覆盖拉取期间已编辑、停用、删除或同步过的账号，重复删除节点不会重复扣减数量。
- 部署、回滚和卸载使用同一主机锁；密钥、流量 Token 和初始密码的自定义路径必须互不相同。
- 更新前先完整复制旧版本快照，避免备份中途失败时回滚误删尚未备份的文件；回滚保留快照源文件，部署收到 HUP、INT、TERM 时恢复旧版本。
- IPv6 回环监听统一用于 Gunicorn、Caddy 和健康检查；重复部署保留已有流量主 Token。
- 首次密码保留首尾空格，登录按原值验证；应用重启后仍能清理默认首次密码文件，无法持久化初始密码时明确失败。
- 流量 Token 使用原子私有文件创建，修复并发首次请求生成不同 Token；非 ASCII 无效 Token 正常返回鉴权失败。
- 流量采集精确匹配端口规则，并以十进制输出大计数；修正 STATUS 中 KB 配额换算，过滤超过 SQLite 整数范围的用量。
- 协议解析统一拒绝无效字段类型、主机和端口，防止异常 VMess 数据造成导入错误。
- 每日备份通过完整校验后才更新 latest 和清理旧备份，失败时保留上一份有效恢复点。

### Tests

- 增加部署故障、凭据持久化和异常订阅回归检查，以及 Ubuntu 24.04 实际安装、IPv6 后端、测试 HTTPS、更新失败恢复和延迟回滚的端到端门禁。

## [1.4.3] - 2026-09-04

### Fixed

- 审计 logger 直接写入进程标准错误流，确保 Gunicorn/systemd 部署中的登录、同步、删除和安全拒绝事件进入 journald。

## [1.4.2] - 2026-09-04

### Fixed

- 删除仍关联用户服务的专线账号时会明确拒绝操作，避免级联删除用户身份、续期历史和稳定订阅链接。
- 订阅同步按协议、主机和端口保留已有节点健康状态，不再把刚检测过的节点全部恢复为“未知”。
- 审计日志显式使用 INFO 级别并接入 Gunicorn 日志处理器，生产操作可以进入 journald。
- 用户服务、续费待办、公开订阅和月度重置统一按 `Asia/Shanghai` 自然日计算；订阅到期时间覆盖完整到期日。
- 续期必须严格延长当前到期日，日期纠错继续通过编辑功能完成。
- 重命名规则现在只改写节点显示名称，不改变协议、地址、端口、密码或查询参数，并兼容 VMess 名称。
- 上游将用量放在标准 `Subscription-Userinfo` 响应头时，面板也能同步上传、下载、总额和到期日。
- 带月度重置日期的绝对流量上报必须声明当前周期，过期周期样本不会污染新周期。
- 固定窗口限流的重载测试锁定时间，消除跨分钟边界造成的偶发失败。

## [1.4.1] - 2026-09-04

### Fixed

- 上游订阅提供流量统计时，同步结果以当前上游用量为准，不再保留月度重置前的历史峰值；没有流量统计的订阅仍保留本地采集值。

## [1.4.0] - 2026-09-03

### Added

- 新增“用户服务”管理：独立记录微信号、服务开始日、到期日、当前专线、关系、备注、提醒状态和续期历史。
- 每位用户拥有自己的稳定订阅链接；迁移到另一条专线时链接不变，暂停、到期或主动轮换后立即停止旧链接访问。
- 仪表盘集中展示未来 30 天续费待办，并在用户有效期超过上游账号有效期时提示续费或迁移。
- 新增不包含业务数据的 JSON 导入工具，可在部署后把私密表格数据幂等导入生产数据库。

### Changed

- 专线账号到期日继续表示真实到期日；系统按该日期的日号推导每月流量重置日，并在短月份自动使用月末。
- 流量周期到达后只重置该账号的本地流量计数，不修改账号到期日；迁移为现有账号保留当前周期数据，避免升级当天误清零。

### Security

- 下游用户数据不会写入源码、Release 或日志；生产导入文件由运维人员在服务器本地限权处理并在完成后删除。
- 用户订阅延续现有随机 Token、限速与无缓存边界，仅在用户和上游账号同时有效时返回节点。

## [1.3.1] - 2026-09-01

### Fixed

- 公开订阅的客户端名称统一为 `SSRVPN.VIP`，不再把面板账号名（例如邮箱）写入订阅响应头。

## [1.3.0] - 2026-08-31

### Added

- 全面更新专业化响应式界面，覆盖登录、仪表盘、账号、账号详情、节点监控和重命名规则，并补齐键盘操作、焦点状态和移动端布局。
- 新增快速开始、配置、API、架构、常见问题、贡献、安全与支持文档，以及结构化 Issue/PR 模板和本地 `make check` 入口。
- GitHub 首页加入真实桌面与移动端界面截图、任务式文档导航、适用范围和清晰的安全默认值说明。
- GitHub Actions 增加最小权限、并发取消、超时和 CodeQL 扩展安全扫描，减少过期任务与代码安全盲区。
- 管理会话增加持久化版本号，修改密码会撤销所有既有会话；首次密码引导文件在改密后自动删除。
- `/readyz` 输出数据库、WAL 与剩余磁盘体积；流量明细按可配置期限自动清理且保留累计流量。
- 请求体、表单字段、SQLite 整数、流量批次和 YAML 解析复杂度增加统一资源上限。
- 新增可校验、自动轮换的数据库与运行密钥灾难恢复备份，并为生产环境提供每日 systemd 定时任务。

### Security

- 订阅默认只允许 HTTPS，节点探测默认只连接公网解析地址；HTTP、私网订阅和私网探测分别使用独立显式开关。
- 退出登录改为带 CSRF 防护的 POST；动态响应默认禁止缓存，会话固定 24 小时期限并逐请求验证撤销状态。
- 节点连接固定到校验后的 IP 并使用共享绝对期限，避免 DNS 重绑定和多地址放大探测时间。
- Gunicorn 升级至 26.2.0，纳入上游 HTTP/2 请求策略安全修复；CodeQL Action 同步升级至 4.37.9。

### Changed

- Gunicorn 改用应用工厂启动，数据库初始化与导入模块解耦；输入限制、节点探测和数据库维护拆分为独立模块。
- CI 同时验证 Python 3.12/3.13，覆盖率门槛提升至 80%，并增加未使用/重复导入检查与新增模块扫描。
- 正式版本的发布、生产部署和公网冒烟测试串联为一条可审计流水线，部署失败时不会把草稿 Release 标记为正式版本。

## [1.2.2] - 2026-08-22

### Fixed

- 订阅源返回确定的 HTTP 错误后立即切换下一个 User-Agent，不再遍历同一主机的全部解析 IP 并耗尽共享的 10 秒端到端期限。
- 全部 User-Agent 失败时返回脱敏的分 UA 原因，不再把 HTTP 拒绝、连接失败和不可解析响应统一误报为“所有 UA 均无法访问”。

## [1.2.1] - 2026-08-22

### Fixed

- 一键部署会把已安装但低于最低受支持版本的 Caddy 从已校验的官方稳定仓库原地升级，不再要求 v1.1.0 用户先手工升级。

## [1.2.0] - 2026-08-22

### Added

- 引入显式版本化、事务化 SQLite 迁移，并持久化登录/API 固定窗口限流状态；限流跨 Gunicorn 进程与重启生效。
- 增加回环专用 `/healthz` 与 `/readyz`，就绪检查覆盖数据库完整性、迁移版本、写锁和剩余磁盘空间。
- 部署成功后保留最近两份带 SHA-256 校验的 LKG 快照，支持 `--list-backups` 和延迟 `--rollback`。
- 记录请求 ID、结构化安全审计事件与健康恢复事件；证书不足 21 天时写入告警。

### Security

- 管理页面启用逐请求 nonce CSP 并移除内联事件处理器；审计日志只记录允许字段，不记录密码、token 或完整订阅 URI。
- systemd 增加 capability、内核、命名空间、地址族和资源上限；批量任务增加数量上限和单任务互斥。
- Caddy 只从官方签名的稳定仓库安装，并强制最低受支持版本。

### Fixed

- 发布标签验证步骤显式注入最小权限 GitHub Token，确保 CI 来源校验可执行。

### Changed

- 正式部署目标收敛为 Ubuntu 24.04、Python 3.12+ 与 systemd；CI 增加 Ubuntu 24.04 真实部署集成门禁及分支覆盖率下限。
- 协议 URI/YAML 转换拆分为注册表驱动的独立 codec，减少核心应用模块的协议分支复杂度；节点检测仍保持 TCP/TLS 语义。
- Caddy 使用默认公开 ACME 签发方和自动续签，不再强制单一 CA。

## [1.1.0] - 2026-08-20

### Added

- 部署脚本自动安装并配置 Caddy，通过 Let’s Encrypt 签发证书、跳转 HTTPS 并自动续签。
- 仪表盘账号概览显示订阅到期日期；systemd 定时器每分钟执行面板与 HTTPS 健康检查。
- GitHub Release 自动生成版本化源码归档、SHA-256，并使用 GitHub Actions OIDC/Sigstore 无密钥签名；仓库启用不可变 Release，发布后锁定 tag 和资产。

### Security

- Root 一键安装器和部署默认源码引用固定到不可变的 `v1.1.0`，不再执行可变 `main`。
- 订阅抓取固定连接到已验证 IP，并在 DNS 解析、所有 User-Agent、重定向及分块读取之间共享 10 秒绝对期限；重定向和错误响应不再读取不受信正文。
- 停用账号的公开订阅 token 立即返回 404，不再继续下发节点凭据。
- 本地部署只按逐文件 `release-files.txt` 清单复制发布文件，不再递归带入 `.secret_key`、数据库或其他 Git 忽略文件。

### Fixed

- 公开订阅只读取最后一次成功同步的数据，不再由外部请求同步拉取上游并占满 Web 线程。
- 混合协议多行订阅按各行真实 scheme 解析；空解析结果会保留旧节点并报告同步失败。
- SQLite 使用明确的 WAL 初始化和写锁等待时间；面板内密码修改与部署统一为 8-128 个字符。
- 部署回滚精确恢复 Panel、Caddy 和健康检查定时器的存在性及 active/enabled 状态，清理由本次部署生成的旧布局数据库副本，并显式报告未完成的回滚步骤。
- 部署前使用 SQLite 在线备份副本验证现有数据库迁移兼容性；源码复制和域名归一化兼容 macOS Bash 3.2/BSD 工具。
- `--keep-data` 帮助文本准确说明会卸载服务配置并保留项目目录和数据库。

### Changed

- 首次交互部署改为要求输入管理员用户名、两次确认密码和面板域名；无人值守模式继续支持对应环境变量。
- Caddy 站点使用独立受管片段，写入前验证域名和配置；卸载时移除对应片段但保留共享 Caddy 服务。
- 生产依赖由 `requirements.in` 生成带 SHA-256 哈希的锁定 `requirements.txt`；本地与 CI 质量门禁使用独立的 `requirements-dev.txt` 哈希锁。

## [1.0.0] - 2026-08-15

### Security

- 拦截订阅拉取对私有、回环、链路本地和保留地址的常见 SSRF，并校验每次重定向和限制响应体为 2 MiB。
- 修复节点监控页由恶意订阅主机名触发的存储型 XSS。
- 生产服务改用专用低权限用户、单进程多线程 Gunicorn 和 systemd 沙箱。
- 增加可配置的 Secure Cookie、可信代理处理、HSTS 和基础安全响应头。
- 私密文件改为排他原子创建，DEBUG 模式仅允许绑定回环地址。
- 流量采集支持账号级 HMAC Token，节点凭据不能跨账号写入。
- 部署默认监听回环并启用 Secure Cookie；安装和卸载拒绝危险、未标记或符号链接目录。
- 部署更新先停服务、重建 root-owned venv，并将可写数据库/密钥隔离到 `data/`，阻断服务账号借升级获得 root 执行。

### Fixed

- 修复克隆部署遗漏 `security_utils.py` 导致服务无法启动。
- 流量上报改为 SQLite 原子自增；采集器只统计自己的规则，并用服务端采集器状态实现重试幂等和跨计数器重置累计。
- 保留 VMess、Shadowsocks、Trojan、VLESS、Hysteria2 等 Clash 转换关键参数，并按 SIP002 输出 SS 插件与 AEAD-2022 URI。
- 修复 Clash YAML 的空 `ws-opts` 导致后续节点被丢弃。
- 独立保存上传、下载、总流量和到期时间，并迁移现有数据库。
- 节点监控明确选择同一地址的最新状态，消除 SQLite 裸列 `GROUP BY` 的不确定性。
- 管理员初始化与数据库迁移可安全应对并发启动。
- 拒绝空或目录型数据库路径、启用 SQLite 外键，并防止删除账号与订阅同步并发时产生孤儿节点。
- 订阅同步和绝对值上报均不再降低本地累计流量；采集器在规则或 ID 持久化失败时停止上报。

### Changed

- 全量订阅同步和批量节点检测改为有界并发网络请求。
- 生产运行最低版本明确为 Python 3.10，并在部署前预检。
- 补充 HTTPS、受信代理、SSRF 例外、备份和回滚运维说明。
