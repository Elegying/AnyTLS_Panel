# AnyTLS Panel 全面审查与修复记录

## v1.4.5：版本入口与更新专项审查

- 左下角从安装文件 `VERSION` 读取当前版本；点击「检查更新」才查询固定 GitHub 正式发布接口，按数字比较版本。查询需要管理员会话和 CSRF，不发送业务数据，也不执行安装。
- 修复显式指定版本仍使用脚本旁旧源码的问题；指定仓库选项后优先拉取，拒绝冒充正式标签的分支和 VERSION 不匹配的源码，检查均在停服前进行。
- 发布门禁新增标签与 VERSION 的一致性验证，保证面板显示与发版一致。
- 一键命令改为私有临时目录完整下载后执行。故障注入证明 HTTP 错误或中途下载失败不会执行部分脚本，会返回失败并清理暂存内容。
- 继续使用现有低权限应用、root 部署、操作锁、数据库副本试迁移、哈希依赖预下载、失败恢复与 LKG 快照。页面检查入口无需增加 sudo 权限；实际升级通过服务器固定版本流程执行。
- 当前 Git 部署依赖 HTTPS 与官方仓库信任，并不验证 Release 归档的 Sigstore 签名；签名归档是另一条可验证的分发路径。断电/SIGKILL 仍需备份恢复，不能当作自动回滚已覆盖。
- 本地 194 项回归通过（192 项通过、2 项 Linux root 检查交由 Ubuntu 门禁），总分支覆盖率 88%，新增版本模块 100%；Flake8、ShellCheck、Bandit、依赖漏洞审计通过。浏览器使用独立测试数据验证桌面、390×500 短屏侧栏滚动、新版本反馈、失败后重试；真实 GitHub 接口查询成功。

新增回归见 [test_updates.py](tests/test_updates.py)；Ubuntu 端到端门禁同时运行新版本检查与既有可靠性回归，再完成真实安装、失败更新恢复和延迟回滚。

审查日期：2026-09-05。基线：v1.4.3、提交 `37a06e6`。修复分支：`codex/deployment-reliability-audit`。

本次覆盖一键部署、环境兼容、依赖安装、订阅与流量对接、初次登录、数据持久化、备份回滚、安全边界、测试与发布配置。改动保持现有 Flask、SQLite、systemd 和协议注册表结构，没有新增运行依赖或数据库迁移。原有本地 `design-qa.md` 保留。

## 修复结果

| 严重程度 | 问题与影响 | 修复与验证 |
| --- | --- | --- |
| 高 | 旧版本逐个搬移到备份目录，中途失败后回滚先清空安装目录，丢失未搬走的文件 | 完整复制成功后才清理旧代码；故障注入确认所有旧文件保留，恢复过程保留备份源 |
| 高 | 终端断开、Ctrl+C 或 SIGTERM 在切换阶段中止部署，旧版本未恢复 | HUP、INT、TERM 共用错误恢复入口；分别验证返回码和旧代码恢复 |
| 高 | 新备份校验前已更新 latest 并轮换旧备份，失败时破坏恢复点 | 校验前不发布 latest、不清理旧备份；增加 Linux 故障注入检查 |
| 中 | `::1` 配置被允许，但 Caddy 和健康检查仍访问 IPv4 | 统一后端地址渲染，IPv6 使用方括号；检查三处实际生成内容 |
| 中 | 更新时环境中的流量主 Token 覆盖已有文件，节点账号级 Token 集体失效 | 已有非空 Token 文件优先保留；回归验证重复部署不替换凭据 |
| 中 | 并发首次流量请求可能生成两个不同 Token | 复用排他创建、0600 权限和 fsync 的私有文件函数；双线程验证只返回同一个持久值 |
| 中 | 登录和首次密码读取会裁剪空格，修改密码时却保留空格 | 按原值验证密码，仅去除密码文件末尾换行；验证初次登录及改密后登录 |
| 中 | 首次密码文件路径只在建库时记录，应用重启后改密不清理该文件 | 启动时确定路径，并向生产 unit 传递自定义路径；验证重载应用后清理 |
| 中 | 无法保存初始密码时仍用不可恢复的随机值创建管理员 | 明确失败并提示检查权限；不再静默创建无法获知密码的账号 |
| 中 | 非 ASCII Token 触发常量时间字符串比较异常并返回 500 | 使用字节比较；无效 Token 正常返回 401 |
| 中 | 443 的流量注释匹配到 4430 等端口，且部分 awk 对大值输出科学计数法 | 完整匹配规则注释，以十进制输出计数；验证相近端口与 TB 级用量 |
| 中 | VMess 节点名称、主机或密码可为列表等类型，端口可越界 | 在协议公共入口验证字段与端口；异常数据不进入数据库绑定或订阅输出 |
| 中 | STATUS 的 KB 配额换算小了 1024 倍，超大用量可超出 SQLite 整数范围 | 修正单位换算并过滤越界计数；验证配额与异常元数据 |

第二轮对抗性审查及远程 Ubuntu 故障注入进一步修复：

| 严重程度 | 问题与影响 | 修复与验证 |
| --- | --- | --- |
| 高 | 回滚重启后端口尚未监听便报告恢复成功 | 启动与恢复等待 `/readyz` 成功，检测持续失败时明确报错 |
| 高 | 特权备份、健康检查在公共可写目录创建可预测锁文件 | 改放备份专用目录和 root 管理的运行目录 |
| 中 | 流量密码传入非字符串类型可触发 SQLite 绑定错误 | 三个上报入口统一拒绝异常类型，返回 400 |
| 高 | 累计流量相加溢出会把 INTEGER 变为浮点数 | 带上限的原子更新，失败回滚整个批次和采集游标 |
| 中 | 接口透传异常文本可能暴露内部错误细节 | 使用固定对外错误文案，注入私密上下文异常验证响应不泄露 |
| 中 | 绝对用量上报的周期检查与写入之间可并发修改或删除账号 | 使用同一写事务保护身份、周期检查和更新 |
| 高 | 网络拉取期间修改、停用或再次同步账号，旧结果仍可能覆盖新状态 | 写事务内核对订阅地址、状态、配额、到期日和同步时间快照 |
| 高 | 并发续期可用旧日期覆盖较晚到期日，续期历史不连续 | 先获取写事务，再核对当前到期日；六线程验证最终日期及历史链 |
| 中 | 并发删除同一节点会重复减少账号节点数 | 仅实际删除成功时更新节点数量 |
| 高 | 同时部署、回滚和卸载会竞争共享代码、服务和 Caddy 配置 | 三个操作共用位于 root 管理的 `/run/` 中的主机锁；隔离 Linux 验证冲突拒绝 |
| 中 | 自定义密钥、流量 Token 和初始密码路径可重合 | 在操作前拒绝所有两两重合的路径 |
| 中 | 用户服务状态接口存在，但页面没有暂停、恢复或停用入口 | 复用现有接口补齐按钮；真实浏览器验证暂停后订阅 404、恢复后原链接 200 |

对应实现见 [deploy.sh](deploy.sh)、[backup.sh](backup.sh)、[app.py](app.py)、[protocol_codecs.py](protocol_codecs.py)、[traffic_collector.sh](traffic_collector.sh)。回归测试集中在 [test_reliability.py](tests/test_reliability.py)。

## 各方面检查结论

| 方面 | 当前结论 | 验证范围与边界 |
| --- | --- | --- |
| 一键部署 | 参数、安装标记、目录归属和服务归属均有检查；先暂存和试装，再切换 | 修复了中途备份失败和信号中断；依赖磁盘仍有足够空间 |
| 系统兼容 | 正式范围为 Ubuntu 24.04 + Python 3.12+ + systemd | 本轮使用真实 Ubuntu 24.04 ARM64 容器及 macOS/Python 3.13；没有扩展 Debian、旧 Ubuntu 或 RHEL 承诺 |
| 依赖安装 | 最小 Ubuntu 实际安装基础工具、venv/pip 和 CA 证书成功 | 哈希锁定、离线切换试装；已完成在线 pip-audit，未发现已知漏洞 |
| HTTPS | 验证了 Caddy 官方源从旧包升级到最低受支持版本、配置验证和 systemd 运行 | 端到端测试使用本地受信任证书；没有进行公网 DNS/ACME 签发或生产域名验收 |
| 订阅对接 | 多协议注册表、Clash/Base64 导入、失败保留上次节点、SSRF 地址固定等路径已有测试 | 修复不合法协议字段和流量元数据；协议支持不等于客户端与真实代理端均已验证 |
| 流量对接 | 账号级 Token 隔离、累计计数幂等、周期样本拒绝已有测试 | 修复端口误计和 Token 持久化；采集器仍只覆盖 IPv4 独占端口 |
| 持久化 | SQLite WAL、外键、迁移、忙等待、流量周期及用户服务稳定链接已有测试 | 本次未改变 schema；补充升级前后数据、Token 和回滚检查 |
| 初次使用 | 随机凭据不会公开输出，部署使用低权限服务用户 | 修复密码空格及重启后清理问题；错误文案与中文文档保持一致 |
| 安全 | CSRF、会话撤销、持久化限流、CSP、SSRF 校验及 systemd 沙箱已检查 | 静态扫描通过；自动测试不能替代外部渗透测试 |
| 代码健康 | 复用现有公共函数、协议注册表和标准库，未引入新框架 | `app.py` 和 `deploy.sh` 仍较大；本次优先修复行为问题，避免机械拆分造成额外风险 |
| 发布 | 检查到精确提交 CI 校验、签名、草稿发布、部署与公网验收门禁 | 本次新增端到端 CI 步骤；最终结果以该版本对应的 CI、Release 工作流和生产验收为准 |

依赖预下载与离线安装机制参照 [pip 官方说明](https://pip.pypa.io/en/stable/cli/pip_download/)。它需要实际试装验证，不能只凭依赖文件存在判断部署成功。

## 验证记录

- 修复前：163 项测试通过，分支覆盖率 86%。这些缺陷通过新增边界测试和故障注入暴露。
- 第一轮修复后：Ubuntu 24.04 ARM64 / Python 3.12 上 176 项测试全部通过；macOS / Python 3.13 上 176 项中 175 项通过，1 项仅适用于 Linux root 的备份检查跳过，该项已在隔离 Linux 中通过。
- 第二轮修复后：本地 Python 3.13 共 183 项测试，181 项通过、2 项 Linux root 专用检查由 Ubuntu 端到端步骤执行；总分支覆盖率 88%，`app.py` 为 85%。
- 真实浏览器完成登录、账号导入、单账号同步、创建用户服务、暂停和恢复；测试使用独立临时数据库。原有生命周期用例继续覆盖编辑、迁移、续期、提醒、轮换、删除、重命名和订阅输出。
- Python 语法、Flake8 关键错误、ShellCheck、Bandit 检查已通过。
- `make audit` 联网执行成功：未发现生产依赖的已知漏洞。
- 现有 [Ubuntu 集成测试](tests/ubuntu24_integration.sh) 已在隔离 Ubuntu 24.04 ARM64 + systemd 中通过，Caddy 从 2.6.2 升级到 2.11.4。
- 新增 [完整部署测试](tests/ubuntu24_e2e.sh) 接入 CI，并已在隔离 Ubuntu 中通过：实际安装、IPv6 后端、受信任测试 HTTPS、备份验证失败、升级时数据库写入故障、自动恢复、正常更新和延迟回滚。各阶段均核对数据库完整性、测试数据和稳定订阅标识，正常更新前后主 Token 哈希一致。
- 修正 Shell 语法门禁：逐个执行 `bash -n`，避免把后续脚本误当作第一个脚本的参数。

## 保留的明确边界

- 写操作和故障注入使用隔离测试数据；生产更新前后核对数据摘要、持久凭据、数据库完整性、公开订阅、服务和备份。客户数据、摘要和凭据不写入仓库。
- 断电和 SIGKILL 不能由 Shell 捕获；应保留异机备份并按运维手册恢复。
- TCP/TLS 探测不能证明代理认证、出口联网或 UDP/QUIC 协议正常。已在 README 中明确表述。
- 流量采集器不支持共享端口按用户计量，也不覆盖 IPv6；跨重启或月度边界的历史流量无法仅靠周期性端口计数精确还原。
- 数据备份仍需做异机加密保存；本地备份无法覆盖整台主机丢失。
