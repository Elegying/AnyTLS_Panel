# 安全策略

AnyTLS Panel 会处理管理员密码、订阅地址、节点凭据、分享 Token 和流量数据。请把安全问题当作私密信息处理。

## 支持版本

| 版本 | 安全更新 |
| --- | --- |
| 最新正式 Release | 支持 |
| 更早版本 | 仅在问题同时影响最新版本时评估，不承诺单独修复 |
| `main` 或功能分支 | 开发中，不作为生产安全支持版本 |

生产环境应运行[最新正式 Release](https://github.com/Elegying/AnyTLS_Panel/releases/latest)，并在升级前完成备份和测试。

## 私下报告漏洞

优先使用仓库 Security 页面中的 **Report a vulnerability**，它会创建仅报告者和维护者可见的私密安全通告：

[私下报告安全问题](https://github.com/Elegying/AnyTLS_Panel/security/advisories/new)

如果该入口暂不可用，请创建一个不含漏洞细节的普通 Issue，只说明“需要安全联系方式”，等待维护者提供私密渠道。不要公开 PoC、真实 Token、订阅内容、服务器地址或可利用步骤。

报告中建议包含：

- 受影响版本和提交 SHA；
- 部署方式和操作系统；
- 问题类型、影响和必要前置条件；
- 使用虚构数据复现的最小步骤；
- 已尝试的缓解措施；
- 是否已在其他位置披露。

## 处理原则

维护者会尽力：

1. 确认收到报告；
2. 复现并评估影响；
3. 与报告者协调修复和披露时间；
4. 在修复可用后发布 Release 和安全说明；
5. 在报告者同意时给予致谢。

响应时间取决于维护者可用性，本项目不提供商业 SLA。请给维护者合理的修复时间后再公开细节。

## 报告范围

优先报告：

- 绕过登录、CSRF、限流或账号级 Token 隔离；
- SSRF、DNS 重绑定、任意网络探测或订阅解析导致的代码执行；
- 存储型或反射型 XSS；
- 部署、更新、回滚或卸载中的提权、路径穿越和任意文件覆盖；
- 密钥、密码、完整订阅 URL 或节点凭据泄露；
- 可绕过资源上限造成稳定拒绝服务的问题。

通常不属于漏洞：

- 只有关闭默认安全设置后才能触发的预期风险；
- 需要已有 `root` 权限才能利用且没有扩大权限的问题；
- 不含敏感信息的版本号或常规服务指纹；
- 社会工程、物理访问或第三方服务自身的问题；
- 没有实际安全影响的自动扫描告警。

## 部署者责任

项目的安全默认值不能代替服务器安全管理。部署者仍需：

- 及时安装系统和 AnyTLS Panel 安全更新；
- 使用强密码并限制管理员访问来源；
- 妥善保护数据库、Token、订阅 URL 和备份；
- 保持 HTTPS、Secure Cookie 和回环绑定；
- 定期验证异机加密备份可恢复；
- 只在隔离且可信的环境中启用私网或 HTTP 例外。
